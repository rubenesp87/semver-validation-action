name: Test action
on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  test:
    name: Test SemVer validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Test action
        uses: rubenesp87/semver-validation-action@aa2049729685535fd4d7dcecf5dd0e771fb22188
        with:
          version: '0.7.9-beta.1'
